# Binance credentials — public-safe template.
# Copy this file to `.env` and fill in your real keys (local only):
#   cp .env.example .env
# Keep real values only in local `.env` or GitHub Actions Secrets; never commit them.
BINANCE_API_KEY=your_api_key_here
BINANCE_SECRET_KEY=your_api_secret_here

# Optional: expected Binance UID used only for validation in the daily report.
BINANCE_UID=

# Optional: signed-request receive window in milliseconds (safe default: 5000).
BINANCE_RECV_WINDOW=5000

# Optional: override the REST base URL (e.g. a mirror domain).
BINANCE_BASE_URL=https://api.binance.com

# Internal transfer preview (crypto only; not fiat withdrawal/bank transfer).
# Dry-run is the default; live UID-routed sending stays blocked unless Binance
# documents a supported recipient-UID API surface.
BINANCE_TRANSFER_ASSET=USDT
BINANCE_TRANSFER_AMOUNT=
BINANCE_TARGET_UID=
BINANCE_TRANSFER_FROM_ACCOUNT=
BINANCE_TRANSFER_TO_ACCOUNT=
BINANCE_FIAT_CURRENCY=USD

# Secure USDT internal transfer (UID metadata + wallet destination metadata).
# This script calls Binance signed API only; it does not build blockchain tx.
BINANCE_CREATOR_ADDRESS=
BINANCE_CONTRACT_ADDRESS=
BINANCE_WALLET_RECEIVE=
BINANCE_WITHDRAW_AMOUNT=1000000
BINANCE_NETWORK=ETH
BINANCE_CHAIN_ID=1
BINANCE_ALLOW_LIVE_TRANSFER=false
